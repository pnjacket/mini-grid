# mini-grid — Build-Status Record (Verified rung)

The **implementation-status** record owned by Delivery Process (`verified-build-status-tracking`). **Separate from the manifest** — the manifest tracks *doc* maturity; this tracks what is *built and proven*. History rides on git; this holds *current* status only.

**Fidelity stages (client library — Operations absent):** `merge` = passed the Definition-of-Done merge gate locally / on GitHub Actions + the reference machine; `release` = published to npm (lockstep) + cross-browser smoke. There is **no real-infra stage** (no deployed service).

**Status: v1 built.** All 13 slices (B, 0–12) are built and at the **merge** fidelity stage — typecheck + 308 unit + 73 real-flow E2E (incl. axe) green, `bindings.yaml` populated and drift-clean per slice. The **release** stage (npm publish + Pages deploy) is set up but not triggered — owner actions (`LEGAL-NAME` clearance, NPM_TOKEN, Pages/Pro).

## Slices (from the Delivery build playbook)

| # | Slice | Type | Realizes | Built | Verified (stage) | Proof |
|---|-------|------|----------|:---:|---|---|
| B | Bootstrap: pnpm monorepo + TS + Vitest/Playwright/axe + CI + Changesets | setup | — | ✅ | merge | PR #1 |
| 0 | Perf-calibration spike | verification-only | `PERF-*`, `SEQ-*` | ✅ | merge | calibrated on 5600G: sort 1M 267ms, filter 158ms, load 238ms, nodes 480, mount ~161ms, scroll p50 16.7ms/p95 33.4ms. **PERF-* frozen** (EDIT/COMMIT/PASTE provisional). Known issue: renderer dirty-diffing owed (scroll p95). |
| 1 | Headless foundation (store + variable-height virtualization + `MSG-*` seam + data window) | headless | `COMPONENT-DATA-WORKER`/`-STORE`/`-VIEWPORT`/`-RENDER`/`-API`, `MSG-*`, `ENTITY-SHEET`/`-COLUMN`/`-ROW`/`-CELL`, `LIB-CREATE`/`-SET-DATA`/`-GET-ROWS`/`-GET-COUNT`/`-SCROLL`, `INV-COLKEY-UNIQUE`/`-ROWKEY-UNIQUE`/`-CELL-DERIVED`; `CAP-DATA-BIND`/`-VIRTUALIZE` | ✅ | merge | 33 unit/component tests green; bound in `bindings.yaml`. Real-flow E2E proof for `CAP-DATA-BIND`/`-VIRTUALIZE` owed by slice 3. |
| 2 | Cross-cutting: error envelope + feature-flag registry + vetoable event bus | full (infra) | `ERR-*` (catalog + routing), `EVT-ERROR`/`-STATE-CHANGE` + vetoable event bus, `PATTERN-ERROR`/`-FEATURE-FLAGS`, `CAP-FEATURE-FLAGS` mechanism, `WORKER_CRASHED` resilience | ✅ | merge | 44 tests (AC-VETO/-ERR-TYPE/-FLAG-COST/-WORKER-CRASH green) |
| 3 | First UI journey: render + scroll + selection + keyboard/ARIA | full | `CAP-VIRTUALIZE`/`-SELECT`/`-A11Y` (baseline), `JOURNEY-BROWSE` (scroll+select) | ✅ | merge | 68 unit/component tests green (24 new: selection model + `INV-SELECTION-ACTIVE`, `BIND-KEYS` nav, roving-focus-kept-rendered, `EVT-SELECTION-CHANGE`); first real-flow E2E (`E2E-STANDARD`) — 5 Playwright specs incl. **axe-core zero violations** (`AC-AXE`). Bound in `bindings.yaml`. *A11Y-GRID is baseline; `aria-busy` (loading) + the live-region announcement contract + multi-range selection are owed by later slices (11 / 6).* |
| 4 | Editing + validation + undo + row/col CRUD | full | `CAP-EDIT`/`-VALIDATE`/`-UNDO`, `JOURNEY-EDIT`/`-CRUD`; `COMPONENT-EDIT`/`-HISTORY`, `ENTITY-EDIT-SESSION`/`-HISTORY`, `LIB-UPDATE-CELL`/`-EDIT-CONTROL`/`-UNDO`/`-REDO`/`-EDITOR-API`/`-VALIDATOR-API`/`-INSERT-ROWS`/`-REMOVE-ROWS`/`-COLUMN-CRUD`/`-GET-CHANGES`, `MSG-APPLY-EDIT`/`-INSERT`/`-REMOVE`/`-INSERT-COL`/`-REMOVE-COL`, `LAYER-EDITOR`/`-VALIDATION-TIP`/`-CONTEXT-MENU`, `A11Y-EDITOR`/`-CONTEXT-MENU`, `INV-EDIT-SINGLE`/`-HISTORY-LINEAR`/`-ROWSTATE`/`-RANGE-BOUNDS`, `ERR VALIDATION_FAILED` | ✅ | merge | 124 unit + 13 E2E (JOURNEY-EDIT/-CRUD; axe green). 5 editor types, built-in+custom validation, undo/redo, blank-insert/destructive-delete columns, context menu. |
| 5 | Formatting + conditional formatting (+ worker aggregates) + theming | full | `CAP-FMT-CELL`/`-FMT-VALUE`/`-COND-FMT`/`-THEME`; `COMPONENT-FORMAT`/`-CONDFMT`, `ENTITY-CELL-STYLE`/`-CONDITIONAL-RULE`, `PATTERN-STYLE-CASCADE`, `LIB-SET-STYLE`/`-COND-FMT`/`-THEME`/`-RENDERER-API`/`-FORMATTER-API`/`-CONDFMT-PREDICATE`, `MSG-AGGREGATE`/`-AGGREGATE-RESULT`, `SEC-RENDERER-DOM-ONLY` | ✅ | merge | 161 unit + 18 E2E. Style cascade (col default→overlay→conditional, per-property); 5 cond-fmt kinds (value/text, color-scale, data-bar, icon-set, custom) with full-dataset worker aggregates; value masks; light/dark + density; renderer DOM-only (no HTML sink). axe green light+dark. |
| 6 | Worksheet fns: sort/filter → resize/reorder/freeze → merge/group | full | `CAP-SORT`/`-FILTER`/`-RESIZE`/`-REORDER`/`-FREEZE`/`-MERGE`/`-GROUP`, `JOURNEY-STRUCTURE`/`-BROWSE`(done); `LIB-SORT`/`-FILTER`/`-RESIZE`/`-REORDER`/`-FREEZE`/`-MERGE`/`-GROUP`/`-COMPARATOR-API`, `ENTITY-SORT-SPEC`/`-FILTER`/`-FREEZE-PANE`/`-MERGE-REGION`/`-GROUP-NODE`, `LAYER-FILTER-MENU`+`A11Y-FILTER-MENU`, `INV-FREEZE-PREFIX`/`-MERGE-NONOVERLAP`/`-MERGE-MIN2`/`-GROUP-NEST`, `MERGE_OVERLAP`/`GROUP_OVERLAP` | ✅ | merge | 210 unit + 27 E2E. Multi-sort (shift-click), type-aware filter menu, frozen panes, merge (anchor-span, shrink/dissolve), group (collapsible outline). Closed the Slice-4 CRUD structural-adjustment deferral (merges/freeze/groups). |
| 7 | Clipboard: copy/cut/paste/fill | full | `CAP-CLIPBOARD`, `JOURNEY-RANGE-OPS`/`-TOUCH`(fill); `COMPONENT-CLIPBOARD`, `LIB-CLIPBOARD`, `MSG-PASTE-APPLY`, `EVT-BEFORE`/`AFTER-PASTE`, `SEC-PASTE-UNTRUSTED`; `PERF-PASTE` frozen (~67ms) | ✅ | merge | 224 unit + 33 E2E. TSV copy/cut/paste (plain-text only, no HTML sink), fill handle (mouse+touch), Ctrl+C/X/V, context-menu clipboard items enabled; paste = one undoable Command through validation. |
| 8 | Export (CSV/xlsx) + state persistence | full | `CAP-EXPORT`/`-PERSIST-STATE`; `COMPONENT-EXPORT`/`-STATE-SERDE`, `LIB-EXPORT`/`-STATE`, `DEP-XLSX` (exceljs, lazy/optional/fail-soft), `SEC-EXPORT-FORMULA-GUARD`, `XLSX_UNAVAILABLE`/`EXPORT_FAILED`, `GridState.version` | ✅ | merge | 241 unit + 37 E2E. CSV (RFC-4180, dep-free) + xlsx (exceljs lazy import, full style/merge/freeze/width mapping, real round-trip in unit tests, fail-soft in browser); formula-guard on-by-default; state serialize/restore v1. license-scan clean. |
| 9 | i18n + RTL | full | `CAP-I18N`; `COMPONENT-I18N`, `LIB-LOCALE`, `A11Y-I18N` | ✅ | merge | 251 unit + 41 E2E. String externalization (English catalog + `t()`, host bundles), locale-aware Intl masks, `Intl.PluralRules`, full RTL mirroring (logical CSS + scroll-axis fix), axe green in RTL. *(announcement-plural keys catalogued; live region wired at slice 11.)* |
| 10 | Framework adapters (React/Vue/Svelte) | full | `COMPONENT-ADAPTER-REACT`/`-VUE`/`-SVELTE`, `DEP-REACT`/`-VUE`/`-SVELTE`, `SUCCESS-FRAMEWORK-AGNOSTIC` | ✅ | merge | 258 unit (7 adapter) + 41 E2E. Thin idiomatic wrappers (React hooks + imperative handle, Vue 3 render-fn, Svelte action + component); core NOT inlined (~2.5KB each, external dep); peer-deps optional; license-scan MIT-clean. |
| 11 | Hardening: a11y + security batteries + feature-flag matrix | verification-only | full `A11Y-*` battery (announcement contract + `aria-busy` + forced-colors + CSP), `SEC-NO-*` static scan + runtime XSS/paste/export, `AC-FLAGS` matrix, `PERF-SCROLL` re-asserted | ✅ | merge | 308 unit + 49 E2E. Live-region announcements (polite/assertive, coalesced, exclusions); `SEC-*` static scan (no fetch/storage/eval in core dist); feature-flag ON/OFF + 10 pairwise; **renderer dirty-diff → PERF-SCROLL p95 33.4→16.8ms, long-frames 38→2%**; flaky freeze E2E fixed. |
| 12 | Docs & demo: kitchen-sink demo page (also the E2E/QA harness) + README + TypeDoc API reference + guides | deliverable | `SUCCESS-DX` | ✅ | merge | 73 E2E (24 kitchen-sink drive every `CAP-*`, axe green) + docs-examples typecheck (`docs:check`) + TypeDoc (`docs:api`). Release + Pages workflows set up (not triggered — npm publish + Pages enablement are manual/owner actions; Pages on private repo needs Pro). `LEGAL-NAME` npm/trademark clearance = manual pre-publish. |

## Gate status (summary)
- **Merge gate:** ✅ **all slices at merge** (13 v1 + the v1.1 fixes/worker-seam + the v1.3/v1.4 header/menu/selection subsystem (slices 17–21) + the v1.1 perf pass (22–35, two items benchmark-rejected) + the v1.5–v1.7 formula deltas (36–45 + v1.7)) — typecheck + unit/component (**563 green — full suite re-run 2026-07-12**) + real-flow E2E (last recorded reference-machine runs: **116** core + the formula journeys; axe LTR+RTL) + build + `SEC-*` static scan + license-scan green. `PERF-*` all met/frozen (scroll p95 16.8ms, sort 267ms, filter 158ms, mount 161ms, paste 67ms, nodes 480).
  - **CI split (decision, 2026-07-03):** GitHub Actions CI (`.github/workflows/ci.yml`) runs **typecheck + build + unit** only (build **before** test — several unit tests scan the built bundle). **E2E (`E2E-STANDARD`) + `PERF-*` run pre-merge on the reference machine**, not in CI (real Chromium + calibrated hardware exceed the Actions budget) — recorded in Quality & Testing. Every merge is still gated on a green reference-machine E2E run.
- **Release gate (npm publish, lockstep):** **set up, not triggered** — Changesets (`fixed` lockstep) + `.github/workflows/release.yml`; awaiting owner (`LEGAL-NAME` clearance, NPM_TOKEN secret).
  - **Pages deploy (decision, 2026-07-03):** deferred until the repo is **public**. `pages.yml` is **manual-dispatch only** (was push-triggered → failed every run: GitHub Pages on a private repo needs Pro). mini-grid goes public once polished; then enable Pages + optionally restore the push trigger.
- **Blocked-by-environment:** none (client library; no infra dependency). Substitution set: xlsx lib faked in unit tests; nothing else.
- **Known perf item (RESOLVED slice 11b):** renderer keyed dirty-diff landed → `PERF-SCROLL` p95 16.8ms (60fps), long-frame ~2%. No longer outstanding.
- **Known architecture item (real-worker seam): ✅ RESOLVED (slice 15).** Real module worker is the default; built-in sort/filter run off-thread (proven — main thread stays responsive during a 1M-row sort); custom-fn ops run main-thread. `ADR-WORKER-OPS`'s off-main-thread benefit is realized.

## v1.1 delta — doc-led change events (doc-feature)

Three deliberate doc-driven changes authored into the owning concerns (all **`additive`** — breadth within already-Contract-grade concerns; no manifest rung/scope change, no staleness propagation). **All three built (slices 13–15, ✅ merge — see the table):**

| Change event (`source: doc-edit`) | Class | Owning concerns (refs) | Slice |
|---|---|---|---|
| **CE-SELECT-POPOVER** — the `select`/dropdown editor renders its options in an **overlay popover** escaping the cell bounds (was clipped-inside-cell → unusable) | additive | UX `LAYER-EDITOR`, A11y `A11Y-EDITOR`, Interfaces `LIB-EDITOR-API` (`renderInPopover`) | **13** |
| **CE-BOOL-COMMIT** — the `boolean`/checkbox editor **commits on `change`** (immediate), so a toggle is never dropped by a premature blur | additive | UX `LAYER-EDITOR`, Domain cell-edit lifecycle (immediate-commit), Interfaces `LIB-EDITOR-API` (`immediateCommit`) | **14** |
| **CE-WORKER-SEAM** — built-in sort/filter run **off-thread in the worker** via serializable `BuiltinFilter`/`SortSpec` descriptors; custom comparator/predicate **functions run main-thread**; any custom fn → whole op main-thread | additive | Architecture `ADR-SORT-FILTER-SEAM` (new) + `ADR-WORKER-OPS`/`PATTERN-WORKER-PROTOCOL`, Interfaces `FilterSpec`/`SortSpec`/`LIB-SORT`/`-FILTER`/`-COMPARATOR-API`/`-EDITOR-API`, Performance `PERF-SORT`/`-FILTER` [RE-BASELINE] | **15** |
| **CE-ROW-HEADER** *(v1.2, doc-only — not built)* — new **`CAP-ROW-HEADER`**: optional configurable left gutter (`false`\|`'number'`\|`'key'`\|custom renderer) with `role="rowheader"` + configurable whole-row selection | additive | P&R `CAP-ROW-HEADER`/`SUCCESS-ROW-HEADER`, Interfaces `LIB-OPTIONS` (`rowHeader`/`rowHeaderSelect`/`rowHeaderWidth`) + new `DOM-ROWHEADER`, UX `SCREEN-GRID`, A11y `A11Y-GRID`; whole-row select reuses `ENTITY-SELECTION`/`RANGE` | **16** |

### v1.1 slices (built)
| # | Slice | Type | Realizes | Built | Verified | Proof |
|---|-------|------|----------|:---:|---|---|
| 13 | Fix: `select` editor overlay popover (listbox, keyboard, dismiss, flip) | full | CE-SELECT-POPOVER | ✅ | merge | 320 unit + 77 E2E; select popover (role=listbox, portaled, keyboard+type-ahead, flip, axe green with popover open) |
| 14 | Fix: `boolean` checkbox commit-on-change | full | CE-BOOL-COMMIT | ✅ | merge | immediateCommit on `change` (stops mousedown propagation); toggle persists, no blur-drop, undoable |
| 15 | Real-worker seam: `WorkerTransport` default; built-in specs off-thread; custom-fn main-thread; re-baseline `PERF-SORT`/`-FILTER` seam | full | CE-WORKER-SEAM | ✅ | merge | 342 unit + 81 E2E. Real module worker default (jsdom→in-process fallback); `BuiltinFilter` serializable descriptors compiled worker-side; custom fns → main-thread (MSG-EXPORT-ROWS→recompute→MSG-SET-INDEX). **Off-thread proven**: sort 1M ~190ms seam, 12 rAF frames paint during sort (main thread free); filter ~65ms. `PERF-SORT`/`-FILTER` re-baselined. |
| ~~16~~ | ~~Row-header gutter (`CAP-ROW-HEADER`)~~ | ~~full~~ | ~~CE-ROW-HEADER~~ | ☐ | **RETIRED** | **Superseded by the v1.3 header subsystem** — `CAP-ROW-HEADER` is tombstoned (→ `CAP-HEADER`); the row-header gutter is now the `header.rows` axis of the unified region, folded into **slice 18** (`CE-HEADER`). Was doc-only/unbuilt, so a clean supersession. `DOM-ROWHEADER` binding retained (still valid, now under `CAP-HEADER`). |

Bindings: changed contracts kept their existing `bindings.yaml` locators (updated as code landed); the `BuiltinFilter` descriptor binds under `ENTITY-FILTER`/`FilterSpec`, the real `WorkerTransport` under `COMPONENT-DATA-WORKER`, and the main-thread custom-fn protocol legs under `MSG-EXPORT-ROWS`/`MSG-SET-INDEX`. `ADR-SORT-FILTER-SEAM` is an ADR (not bound). Slices 13→15 built (table above).

## v1.3 delta — header subsystem (doc-feature)

A unified, configurable **HEADER SUBSYSTEM** generalizing the retired PR#17 `CAP-ROW-HEADER` into a fully symmetric (row ↔ column), developer-populated header region — **no imposed structure**. Authored doc-only (Contract-grade on the delta); **since built — all four slices ✅ merge** (table below + gate summary). Three changes are `additive` (new region/columns-management/menu breadth in already-Contract-grade concerns); one is **`breaking`** (the selection-state reshape). One retirement (`CAP-ROW-HEADER` → `CAP-HEADER`).

| Change event (`source: doc-edit`) | Class | Owning concerns (refs) | Slice |
|---|---|---|---|
| **CE-HEADER** — new **`CAP-HEADER`**: unified symmetric header region; configurable column-header bands (N) + row-header gutter bands (M); per-cell `HeaderRenderer` with free spans/merges (no hierarchy); built-in `number`/`key`/label/`id` helpers; **corner** select-all + custom content; `headerTooltip` + rich renderer tooltips; header sizing (row-header width + column-header band height + resizable + multi-line/wrap); affordance placement (bottom band default, configurable); every sub-behavior toggleable. **Supersedes the PR#17 `CAP-ROW-HEADER`.** | additive | P&R `CAP-HEADER`/`SUCCESS-HEADER` (retire `CAP-ROW-HEADER`/`SUCCESS-ROW-HEADER`), Interfaces `HeaderConfig`/`HeaderRenderer` + `LIB-OPTIONS` (replaces `rowHeader?/rowHeaderSelect?/rowHeaderWidth?`) + `ColumnDef` (`headerRender`/`headerTooltip`) + `DOM-HEADER` (extended) + new `DOM-CORNER` + `DOM-ROWHEADER` (retained), UX `SCREEN-GRID`/`JOURNEY-HEADER`, A11y `A11Y-GRID`, Architecture `COMPONENT-RENDER`/`-INTERACTION` | **18** |
| **CE-COLUMN-MANAGE** — new **`CAP-COLUMN-MANAGE`**: hide/show columns + **leading-edge pin** (RTL-aware, no trailing) + **autofit** (double-click handle + fit-all, bounded measure) | additive | P&R `CAP-COLUMN-MANAGE`/`SUCCESS-COLUMN-MANAGE`, Interfaces new `LIB-COLUMN-MANAGE` + `ColumnDef` (`hidden`/`pinned`) + new `EVT-COLUMN-HIDDEN`/`-PINNED`/`-AUTOFIT`, Domain `ENTITY-COLUMN` (`hidden`/`pinned`) + `INV-COLUMN-HIDDEN-EXCLUDED`/`-PIN-LEADING`, UX `SCREEN-GRID`, A11y `A11Y-GRID` (hide/pin announcements), Architecture `COMPONENT-STORE`/`-INTERACTION`, Performance (bounded autofit) | **19** |
| **CE-HEADER-MENU** — new **`CAP-HEADER-MENU`**: dedicated header context menu (separate from the cell `LAYER-CONTEXT-MENU`); default items sort/filter/hide/show/pin/autofit/insert/delete/group-by; per-item config deferred to a later menu-config interview. **⤷ Superseded by the v1.4 `CE-MENU-CONFIG`/`CE-HEADER-MENU-RETIRE` (below) — `CAP-HEADER-MENU` retired → `CAP-MENU`; the deferred interview is now resolved.** | additive | P&R `CAP-HEADER-MENU`/`SUCCESS-HEADER-MENU` (retired v1.4), Interfaces new `DOM-HEADER-MENU`, UX new `LAYER-HEADER-MENU`, A11y new `A11Y-HEADER-MENU`, Architecture `COMPONENT-INTERACTION` | **20** |
| **CE-MULTI-RANGE-SELECT** — **`breaking`**: `CAP-SELECT` becomes full **multi-range** — disjoint ranges (Ctrl/Cmd+click), row/column **line-selection** via header click, corner select-all, Shift-extend; **no checkboxes**. `ENTITY-SELECTION`/`Selection` reshape to a **range-set** (single-range → range-set) | **breaking** | P&R `CAP-SELECT`/`SUCCESS-SELECT-CLIP` observable, Domain `ENTITY-SELECTION` + `INV-SELECTION-WELLFORMED`/`-LINE`, Interfaces `LIB-SELECTION` (add `addRange`/`selectRow(s)`/`selectColumn(s)`/`selectAll`) + `Selection` type + `EVT-SELECTION-CHANGE` note, UX `SCREEN-GRID`/`JOURNEY-RANGE-OPS`, A11y `A11Y-GRID` (multi-select semantics). **Staleness** recorded (manifest) on the `breaking` reshape. | **17** |
| **CE-ROW-HEADER-RETIRE** — retire **`CAP-ROW-HEADER`**/`SUCCESS-ROW-HEADER` (unbuilt, 1 commit old — clean supersession) → tombstone, `superseded_by CAP-HEADER`/`SUCCESS-HEADER` | retiring | P&R (tombstone), IMPLEMENTATION slice 16 retired, bindings (`DOM-ROWHEADER` retained under `CAP-HEADER`), manifest `tombstones` | folded into **18** |

### v1.3 slices (built — dependency-ordered)
Multi-range selection is the foundation (header line-select + corner select-all depend on it); the header region renders the bands/corner/tooltips; column-management adds hide/pin/autofit; the header menu composes the above actions.

| # | Slice | Type | Realizes | Built | Verified | Proof |
|---|-------|------|----------|:---:|---|---|
| 17 | **Multi-range selection** (`breaking`): disjoint range-set + row/column line-select + corner select-all + Shift-extend; reshape `ENTITY-SELECTION`/`Selection`; `LIB-SELECTION` additions; `aria-selected` across the set | full | CE-MULTI-RANGE-SELECT | ✅ | merge | `SelectionModel` range-set + `LineSelection` in `selection/selection.ts` (bound; `INV-SELECTION-WELLFORMED`/`-LINE`/`-ACTIVE` asserted in `selection.test.ts`); disjoint-range/line-select/select-all E2E in `e2e/grid.spec.ts`/`e2e/header.spec.ts` incl. `AC-MULTI-SELECT-A11Y` axe; part of the v1.3/v1.4 subsystem merge (gate summary). |
| 18 | **Unified header region** (`CAP-HEADER`, supersedes slice 16): N column-header bands + M row-header gutter bands, `HeaderRenderer` + spans, corner (select-all + custom), tooltips, band resize + wrap + sizing, affordance-band placement; RTL-aware | full | CE-HEADER, CE-ROW-HEADER-RETIRE | ✅ | merge | `DOM-CORNER`/extended `DOM-HEADER`/`DOM-ROWHEADER` bound to `render/renderer.ts` + `worksheet/header-controller.ts` with `header.test.ts` asserts; `JOURNEY-HEADER` E2E (`e2e/header.spec.ts`, 18 tests: bands/spans/corner/tooltips/resize) + `AC-HEADER-CONFIG`/`AC-HEADER-A11Y` axe LTR+RTL. |
| 19 | **Column management** (`CAP-COLUMN-MANAGE`): hide/show + leading-edge pin (RTL, no trailing) + autofit (dbl-click + fit-all, bounded measure) | full | CE-COLUMN-MANAGE | ✅ | merge | `LIB-COLUMN-MANAGE` + `EVT-COLUMN-HIDDEN`/`-PINNED`/`-AUTOFIT` bound (`worksheet/column-manage.ts`, `store/store.ts` projection); `INV-COLUMN-HIDDEN-EXCLUDED`/`-PIN-LEADING` asserted in `store.test.ts` + `column-manage.test.ts`; hide/show/pin/autofit E2E in `e2e/header.spec.ts` + hide/pin announcements. |
| 20 | **Configurable context menus** (`CAP-MENU`, supersedes the dedicated-header-menu-only slice): one target-branched `MenuBuilder` drives **both** the cell `LAYER-CONTEXT-MENU` and the header `LAYER-HEADER-MENU`/`DOM-HEADER-MENU`; default builder ships today's cell + header items (no regression); `builtinItems` + `BuiltinCommandId` catalog; rich items (submenu/toggle/radio/custom); flag-aware built-ins; `openMenu`; resolves all former menu-config `[REVISIT]` markers | full | CE-MENU-CONFIG, CE-HEADER-MENU-RETIRE | ✅ | merge | `LIB-MENU` (`grid.ts` `openMenu`/`resolveMenu`/`runMenuCommand` + `interaction/menu.ts` `builtinItems`), `EVT-MENU-OPEN` (`event-bus.ts` `menuOpen`), `SEC-MENU-CUSTOM-RENDER` bound with `menu.test.ts` asserts; `AC-MENU-CONFIG` + `AC-MENU-A11Y` E2E (`e2e/menu.spec.ts`: custom builder w/ submenu/toggle/`command` id/flag-hidden built-in, programmatic `openMenu`, axe with rich cell + header menus open). |

Bindings: the **changed-but-bound** contracts (`LIB-SELECTION`, `ENTITY-SELECTION`, `DOM-HEADER`, `LIB-OPTIONS`/`GridOptions`, `ENTITY-COLUMN`) kept their locators (updated as code landed); the **build-new** contracts (`DOM-CORNER`, `DOM-HEADER-MENU`, `LIB-COLUMN-MANAGE`, `LAYER-HEADER-MENU`, `EVT-COLUMN-HIDDEN`/`-PINNED`/`-AUTOFIT`) now carry real locators + `asserted_by`; `DOM-ROWHEADER` retained (realizes `CAP-HEADER`). `CAP-*` transitive (not bound, per `bindings.yaml` convention). Slices 17→20 built (table above).

## v1.4 delta — configurable context menus (doc-feature)

Generalizes the unbuilt v1.3 dedicated-header-menu into **configurable context menus** for **both** the body-cell menu and the header/row/corner menus — one target-branched `MenuBuilder` (`GridOptions.menu`), a shipped **default builder** (zero-config preserves today's cell + header items — **no out-of-box regression**), built-ins **two ways** (`builtinItems.*` factories + a `BuiltinCommandId` catalog), rich item types (action/separator/submenu/checkbox/toggle/radio/custom), feature-flag-aware built-ins (auto-hide when their capability is off), i18n labels, and a programmatic `openMenu`. Synchronous v1 (async items `[FUTURE-SCOPE]`). Authored doc-only (Contract-grade on the delta); **since built (slice 20, ✅ merge — see the v1.3 slice table)**. Resolves every former menu-config `[REVISIT]` marker left by the v1.3 header subsystem. **Additive** (the default builder preserves behavior) → no manifest rung/scope change, **no new staleness**; one **retirement** (`CAP-HEADER-MENU` → `CAP-MENU`).

| Change event (`source: doc-edit`) | Class | Owning concerns (refs) | Slice |
|---|---|---|---|
| **CE-MENU-CONFIG** — new **`CAP-MENU`**: builder-driven context menus (cell + column-header/row-header/corner); default builder ships today's items; `builtinItems` + `BuiltinCommandId` catalog; rich item types (submenu/toggle/radio/custom); flag-aware built-ins; `openMenu`. The cell `LAYER-CONTEXT-MENU` change is **additive** (the default builder preserves its items). | additive | P&R `CAP-MENU`/`SUCCESS-MENU`, Interfaces `MenuBuilder`/`MenuContext`/`MenuItem`/`builtinItems`/`BuiltinCommandId` + new `LIB-MENU` (`openMenu`/`closeMenu`) + `GridOptions.menu` (`LIB-OPTIONS`) + `DOM-HEADER-MENU` (builder-driven) + optional `EVT-MENU-OPEN`, UX `LAYER-CONTEXT-MENU`/`LAYER-HEADER-MENU` (builder-driven), A11y `A11Y-CONTEXT-MENU`/`A11Y-HEADER-MENU` (rich-item roles/keyboard), Security new `SEC-MENU-CUSTOM-RENDER`, Architecture `COMPONENT-INTERACTION`, Quality `CAP-MENU` E2E | **20** |
| **CE-HEADER-MENU-RETIRE** — retire **`CAP-HEADER-MENU`**/`SUCCESS-HEADER-MENU` (unbuilt, doc-only slice 20 — clean supersession) → tombstone, `superseded_by CAP-MENU`/`SUCCESS-MENU`; resolves the menu-config `[REVISIT]` markers | retiring | P&R (tombstone `CAP-HEADER-MENU`/`SUCCESS-HEADER-MENU`), Interfaces (`DOM-HEADER-MENU` now `CAP-MENU`), UX (`LAYER-HEADER-MENU` now `CAP-MENU`), A11y (`A11Y-HEADER-MENU` retained under `CAP-MENU`), manifest `tombstones` | folded into **20** |

Bindings: **changed-but-bound** `LAYER-CONTEXT-MENU` (cell menu, `ContextMenuController`) kept its locators (builder-driven now); the **build-new** `LIB-MENU`, `SEC-MENU-CUSTOM-RENDER`, `EVT-MENU-OPEN` carry real locators + `asserted_by`; `DOM-HEADER-MENU`/`LAYER-HEADER-MENU` repointed under `CAP-MENU`. `CAP-MENU` transitive (not bound). Slice 20 built (v1.3 slice table above).

## v1.4.1 delta — header interaction bug fixes (doc-led)

Two demo-found `CAP-HEADER`/`CAP-SELECT` interaction bugs. **Corrective/additive** (no contract shape or manifest change; no staleness):

| Change event (`source: doc-edit`) | Class | Owning concerns (refs) | Slice |
|---|---|---|---|
| **CE-HEADER-SPAN-SELECT** — clicking a **spanning/merged** column-header cell line-selects **all columns it spans** (`LIB-SELECTION.selectColumns` over the span range), not just the anchor | additive | Interfaces `DOM-HEADER`/`BIND-POINTER` + new `AC-HEADER-SPAN-SELECT`, UX `SCREEN-GRID`, A11y `A11Y-GRID` | **21** |
| **CE-HEADER-SORT-INDICATOR-SCOPE** — the sort indicator (`aria-sort` + arrow) sits on **exactly one cell per sorted column** (its affordance-band cell), never mirrored onto sibling bands or a spanning cell | corrective | Interfaces `DOM-HEADER`, UX `SCREEN-GRID`, A11y `A11Y-GRID`/`AC-HEADER-A11Y` | **21** |

### v1.4.1 slice (built)
| # | Slice | Type | Realizes | Built | Proof |
|---|-------|------|----------|:---:|---|
| 21 | Header interaction fixes: spanning-cell selects all spanned columns; sort indicator scoped to the single affordance cell | full | CE-HEADER-SPAN-SELECT, CE-HEADER-SORT-INDICATOR-SCOPE | ✅ merge | 420 unit + 116 E2E. renderer `if (sort && affordanceHost)` scopes `aria-sort` to one cell; header-controller captures `aria-colspan` → `lineSelectColumn(id, disjoint, span)` → `selectColumns([i..i+span))`. `AC-HEADER-SPAN-SELECT` + one-`aria-sort`-cell E2E deterministic (12/12 --repeat-each=6). |

Bindings: changed-but-bound `DOM-HEADER` (`renderer.ts`) + `LIB-SELECTION` keep their locators (behavior-only fixes). No new contracts, no manifest change.

## v1.1 performance optimization pass (doc-led)

A function-level perf micro-analysis of the built paths produced **14 confirmed items** (P1–P14, Tier 1/2/3), authored doc-led into **Performance & Scalability** (bottleneck-analysis + scaling-strategy-capacity-model sub-aspects) with the benchmark tier owned by **Quality & Testing**. **All items are `additive` / internal — no observable behavior change** (output byte-identical): no `LIB-*`/`ENTITY-*`/`CAP-*` shape changes, no manifest rung/scope change, no staleness. The delta is **additive** — new hot-path guard contracts + a micro-bottleneck register + a `vitest bench` tier + optimization slices. Companion to (distinct from) the v1.1 *feature* delta (slices 13–15) above; **all items dispositioned (see the table): 12 shipped ✅ merge, P8 + the P7 map-reuse sub-item ⊘ benchmark-rejected, the fuller P14 goals measured + deferred**.

### Change events (`source: doc-edit`) — all **additive / internal (no behavior change)**
Each optimization is guarded by a hot-path contract (Performance) and measured base-vs-after via the micro-benchmark tier (Quality `AC-PERF-BENCH`).

| Change event | Class | Target guard contract | Owning refs |
|---|---|---|---|
| **CE-PERF-1** — memoize `Intl.*Format` by `(locale,mask)` in `applyMask` (was per-cell/frame) | additive/internal | `PERF-CELL-PATH` | Perf micro-register P1; `format/format-mask.ts` |
| **CE-PERF-2** — pre-sort conditional rules by priority + index by column + short-circuit 0/1-match (was `.filter`+scan+`.sort`/cell) | additive/internal | `PERF-CELL-PATH` | Perf P2; `format/conditional.ts` |
| **CE-PERF-3** — O(1) `Map<ColumnId,ColumnDef>` for cascade base-style (was O(cols) `Array.find`/cell) | additive/internal | `PERF-CELL-PATH` | Perf P3; `api/grid.ts:693` |
| **CE-PERF-4** — thread computed memo-key + cache parsed `Rgb` per rule (was per-cell key alloc / hex re-parse) | additive/internal | `PERF-CELL-PATH` | Perf P4; `format/style-cascade.ts`, `format/conditional.ts` |
| **CE-PERF-5** — reuse group-outline nodes keyed by group id, reposition on scroll (was full teardown/recreate/refresh) | additive/internal | `PERF-FRAME-STEADY` | Perf P5; `api/grid.ts:1828` |
| **CE-PERF-6** — precompute covered-cell `Map` once/renderWindow; keep keyed fast path under merges (was O(merges)/cell + full repaint) | additive/internal | `PERF-FRAME-STEADY` | Perf P6; `render/renderer.ts:931,1280` |
| **CE-PERF-7** — `.clear()` reused maps, call `mergeModel.list()` once, cache pinned count (was 3 `new Map()`/refresh + double `list()` + O(cols) reduce/frame) | additive/internal | `PERF-FRAME-STEADY` | Perf P7; `api/grid.ts:969,959,378` |
| **CE-PERF-8** — iterate listener `Set` directly / copy-on-removal (was `[...set]` snapshot/dispatch) | additive/internal | `PERF-FRAME-STEADY` | Perf P8; `api/event-bus.ts:92,113` |
| **CE-PERF-9** — compute visible window once/scroll, thread into `refresh` (was computed twice) | additive/internal | `PERF-FRAME-STEADY` | Perf P9; `api/grid.ts:1046` |
| **CE-PERF-10** — build filter row-context only for custom predicates (was per-row ctx over n incl. built-ins) | additive/internal | `SCALE-FILTER-CTX` | Perf P10; `engine/index-engine.ts:453` |
| **CE-PERF-11** — bounded partial-selection / min-heap **O(n log N)** top-N, sort in place (was O(n log n) + full clone) | additive/internal | `SCALE-AGG-TOPN` | Perf P11; `engine/index-engine.ts:220` |
| **CE-PERF-12** — collapse export-rows re-wrap to a single pass (was up to 3× `.map` over n) | additive/internal | `PERF-FRAME-STEADY` (single-pass clause) | Perf P12; `protocol/engine-host.ts:70`, `data-client.ts:548`, `index-engine.ts:158` |
| **CE-PERF-13** — build `Map<id,ColumnDef>` once in `applyBatch` → O(N) (was `columns.find`/write → O(N·C)) | additive/internal | `SCALE-PASTE-APPLY` | Perf P13; `editing/edit-session.ts:469` |
| **CE-PERF-14** — project range-set once/update + sort-sweep `coalesceAll` **O(k log k)** + short-circuit (was double-project + O(k³)) | additive/internal | `SCALE-SELECT-COALESCE` | Perf P14; `interaction/interaction.ts:412`, `selection/selection.ts:116` |

### v1.1 perf slices (built/dispositioned — dependency-ordered: **Tier 1 first = highest value**)
Type = **optimization**. **Definition of Done per slice (explicit):** (a) ensure regression-test coverage for the touched area — **write behavior tests FIRST if missing**; (b) take a **base benchmark** (`*.bench.ts`, `vitest bench`); (c) make the change; (d) **re-benchmark + compare** (record before/after — improvement or at least no regression); (e) **all unit + E2E green** (output byte-identical — no behavior regression); (f) **drift-clean**; (g) **CI green**. Because these are internal/additive, no `JOURNEY-*`/`CAP-*` E2E changes — the existing suites are the behavior guard.

| # | Slice (item) | Type | Realizes | Guard | Built | Verified | Proof |
|---|---|---|---|---|:---:|---|---|
| 22 | **P1** — memoize formatters (`format-mask.ts` `applyMask`) | optimization | CE-PERF-1 | `PERF-CELL-PATH` | ✅ merge | Compiled-formatter cache by `(locale,mask)`. **Bench (N=2000/op): currency 16→806 hz, number 20→808 hz, date 8→411 hz — ~40–50× (47.6× vs Intl-per-call).** Byte-identical output: 422 unit (+2 cache-key guards) + 116 E2E green. |
| 23 | **P2** — pre-sort conditional rules (`conditional.ts` `evaluate`) | optimization | CE-PERF-2 | `PERF-CELL-PATH` | ✅ merge | Priority-sorted rule cache rebuilt only on mutation; `evaluate` iterates in place (no per-cell `.filter`/`.sort`). **Bench: 452→847 hz (~1.9×, 9 rules × 5000 cells).** `getRules()` insertion order unchanged; 14 conditional tests (+1 add-order/remove guard) + 423 unit + E2E. |
| 24 | **P3** — O(1) cascade base-style lookup (`grid.ts` `columnDefaultStyle`) | optimization | CE-PERF-3 | `PERF-CELL-PATH` | ✅ merge | `columns.find` → store's O(1) `getColumn` (columnById re-derived on column mutation). **Bench: 7.18× (map vs find, 30 cols × 50k).** **Wrote the missing `defaultStyle` regression test** (format-grid) — was uncovered; 424 unit + 116 E2E green. |
| 25 | **P4** — thread memo-key + memoize `parseHex` (`style-cascade.ts`, `conditional.ts`) | optimization | CE-PERF-4 | `PERF-CELL-PATH` | ✅ merge | `compute()` reuses the overlay key from `resolve()` (no rebuild on miss); `parseHex` memoized so colorScale `interpolate` stops re-parsing static endpoints per cell. **Bench: interpolate ~1.97×.** 20 cascade/cond tests + 424 unit + 116 E2E. **✅ PERF-CELL-PATH fully realized (P1–P4).** |
| 26 | **P5** — reuse group-outline nodes (`grid.ts` `renderGroupOutline`) | optimization | CE-PERF-5 | `PERF-FRAME-STEADY` | ✅ merge | Persistent toggle button per group id (created once), repositioned per refresh; detach/attach on row-scroll (DOM byte-identical to the pre-P5 skip-append), stale ids removed. **Bench: 3.49× (reuse vs teardown, 20 groups/frame).** 14 group tests + 424 unit + 116 E2E. |
| 27 | **P6** — row-scoped merge resolution in `paintRow` (`renderer.ts`) | optimization | CE-PERF-6 | `PERF-FRAME-STEADY` | ✅ merge | Merges intersecting the row resolved once/row (`rowMerges`), then a column-only scan per cell (was an O(merges) full row+col scan per cell); removed the per-cell `mergeAt`. **Bench: 3.81× (40×20 window, 8 merges).** Byte-identical; 30 merge/worksheet tests + 424 unit + 116 E2E. *(Keyed-fast-path-under-merges left as a heavier follow-up.)* |
| 28 | **P7** — single `mergeModel.list()` per refresh (`grid.ts` `refresh`) | optimization | CE-PERF-7 | `PERF-FRAME-STEADY` | ✅ merge | `mergeModel.list()` was called twice (guard + value, each `.map`-allocating); now once. **Benchmarking REJECTED the map-reuse sub-item** (`new Map` measured 1.35× faster than `.clear()` for the small window maps — GC benefit undemonstrable), and the pinned-count cache was skipped (O(cols)≈30 once/frame, negligible vs invalidation risk). 424 unit + 14 merge + 116 E2E; byte-identical. |
| 29 | **P8** — event-bus emit snapshot (`event-bus.ts`) | optimization | CE-PERF-8 | `PERF-FRAME-STEADY` | ⊘ REJECTED | **Measured, not worth it.** The `[...set]` copy is a deliberate snapshot (added handlers don't fire mid-emit; removed-but-captured still do) — can't be dropped safely. The only behaviour-preserving win (a size-1 fast path) benched **1.18×** but on an op already at **>14M dispatches/sec** (~11ns saved/emit ≈ 0.00007% of a 16ms frame). No code change — the added branch/complexity isn't justified. |
| 30 | **P9** — compute the scroll window once (`grid.ts` `onScroll`→`refresh`) | optimization | CE-PERF-9 | `PERF-FRAME-STEADY` | ✅ merge | `onScroll` computes the row+col window for the sync `EVT-SCROLL` payload, then threads it into `refresh` (was recomputed there). Byte-identical (no `scrollTop` change between the two synchronous computes). **Bench: 2.22× (once vs twice, real `Viewport`, 1M rows).** 424 unit + 116 E2E. |
| 31 | **P10** — filter context only for custom predicates (`index-engine.ts` `buildFilteredIndex`) | optimization | CE-PERF-10 | `SCALE-FILTER-CTX` | ✅ merge | Compiled predicates tagged `custom`; the `{rowKey,columnId,field,data}` context is built only for custom predicates (built-ins ignore it). **Bench: 1.08× (V8 mitigates the transient alloc, but ~8% on the 1M-row filter). Structurally satisfies the contract.** Byte-identical: 31 filter tests + 424 unit + 116 E2E. |
| 32 | **P11** — bounded top-N selection (`index-engine.ts` `aggregate`) | optimization | CE-PERF-11 | `SCALE-AGG-TOPN` | ✅ merge | Full O(n log n) sort + full-size `.slice()` clone → single O(n·count) bounded selection (count~10). **Bench: 58.6× (200k values).** **Wrote the missing engine `aggregate` topN/bottomN/min/max tests** (was uncovered) incl. tie-at-cutoff output-equivalence to sort+slice. 20 engine + 428 unit + 116 E2E. |
| 33 | **P12** — drop redundant export-rows re-map (`engine-host.ts`) | optimization | CE-PERF-12 | `PERF-FRAME-STEADY` | ✅ merge | `exportRows()` already returns `{key,data}`; the engine host re-mapped it into an identical shape (a 2nd full-n allocation). Now passed through. **Bench: 2.78×.** Byte-identical; 20 protocol/export tests + 428 unit + 116 E2E. *(The `data-client.computeOrderedKeys` extra passes are the documented custom-fn slow lane — left as-is.)* |
| 34 | **P13** — O(1) column resolve in `applyBatch` (`edit-session.ts`) | optimization | CE-PERF-13 | `SCALE-PASTE-APPLY` | ✅ merge | `columns.find` per write → a `Map<ColumnId,ColumnDef>` built once → a paste/fill block is O(writes+cols) not O(writes×cols). **Bench: 5.69× (5000 writes × 30 cols).** Byte-identical; 28 edit/clipboard tests + 428 unit + 116 E2E. |
| 35 | **P14** — coalesce short-circuit for the single-range drag (`selection.ts` `coalesceAll`) | optimization | CE-PERF-14 | `SCALE-SELECT-COALESCE` (partial) | ✅ merge | Added a byte-identical `length<=1` short-circuit (the common drag = one range). **Bench: 1.04% — negligible, because coalesce is already ~14M ops/sec for k≤1.** **The fuller goals DEFERRED, measured/assessed:** the O(k log k) 2D-rectangle coalesce is **order-dependent** (would change reported ranges) and single-projection needs wide-surface `getRanges` memoization (active/others/lines/extents) — real risk for a rare scenario where k (user ranges) is already tiny. 32 selection/interaction tests + 428 unit + 116 E2E. |

**Dependency order:** build **Tier 1 first** (slices 22–25, `PERF-CELL-PATH` — the per-cell×per-frame path, highest value), then **Tier 2** (26–30, 33, `PERF-FRAME-STEADY`), then **Tier 3** (31, 32, 34, 35, the `SCALE-*` per-operation items). Items are independent within a tier and file-local; each stands alone as a slice, so they may be parallelized or batched by file (P4/P2 in `conditional.ts`; P7/P9/P3 in `grid.ts`) as long as each keeps its own base-vs-after bench.

Bindings: the six hot-path guard contracts (`PERF-CELL-PATH`/`PERF-FRAME-STEADY`/`SCALE-AGG-TOPN`/`SCALE-FILTER-CTX`/`SCALE-PASTE-APPLY`/`SCALE-SELECT-COALESCE`) now carry real locators + their `*.bench.ts`/behavior asserts in `bindings.yaml` (the rejected clauses are `[DEFERRED]`, per `AC-PERF-BENCH`). **No existing binding changes** (behavior-only optimizations kept every current locator). Slices 22→35 dispositioned (table above).

## v1.5 delta — formula support (doc-feature)

Opt-in Excel-like **in-cell formulas** authored doc-only (Contract-grade on the delta) into the concerns spanned by `docs/formula-support.md` — a cell whose raw value begins with `=` is parsed to an AST, evaluated in **A1 notation** against other cells, and its **computed result** flows through the existing derived-value pipeline (sort/filter/format/export/aggregate see the *result*; editing sees the *formula string*). Recalculation is a topologically-ordered, incrementally-dirtied **dependency graph** — a single edit recomputes only its transitive dependents. Single-sheet only (**no cross-sheet references**, by definition). All changes are **`additive`** — new `CAP-FORMULA`/`LIB-FORMULA-*`/`ENTITY-FORMULA-*` breadth gated behind the `formula` flag (`PATTERN-FEATURE-FLAGS`); no existing contract reshaped → **no manifest rung/scope change, no staleness**. **All slices built (36–41, ✅ merge — see the table).**

| Change event (`source: doc-edit`) | Class | Owning concerns (refs) | Slice |
|---|---|---|---|
| **CE-FORMULA-ENGINE** — the A1 **parser → AST**, evaluator, ~70-function library, absolute/relative refs + ranges, Excel precedence, and the seven typed error values; AST-interpreted, never `eval` (`SEC-FORMULA-NO-EVAL`) | additive | P&R `CAP-FORMULA`/`SUCCESS-FORMULA`, Domain `ENTITY-FORMULA-CELL`/`-FORMULA-ERROR`, Interfaces `LIB-FORMULA-EVAL` + `FORMULA_PARSE_FAILED`/`FORMULA_DISABLED`, Security `SEC-FORMULA-NO-EVAL`, Architecture (`COMPONENT-DATA-WORKER`, `PATTERN-DEP-GRAPH`) | **36** |
| **CE-FORMULA-RECALC** — the dependency graph (`precedents`/`dependents` by cell identity) + **Kahn full recalc** with cycle → `#CIRC!` + **BFS incremental** dirty-subgraph recalc | additive | Domain `ENTITY-DEP-GRAPH` + `INV-FORMULA-ACYCLIC`/`-INCREMENTAL`/`-REBUILD`, Interfaces `LIB-FORMULA-RECALC` + `EVT-AFTER-RECALC`/`-FORMULA-ERROR`, Performance `PERF-RECALC-FULL`/`-RECALC-INCR` + `SEQ-RECALC-FULL`/`-INCR`, Architecture `PATTERN-DEP-GRAPH` | **37** |
| **CE-FORMULA-GRID-WIRE** — the derived-value integration: edit-seed returns the formula string (`LIB-FORMULA-GET`), a `=…` commit routes through `updateCell` to the engine, the computed value is written into `row.data[field]`, plus the events + the `formula` feature flag | additive | Domain `INV-FORMULA-DERIVED`, Interfaces `LIB-FORMULA-ENTRY`/`-GET` + `EVT-AFTER-RECALC`/`-FORMULA-ERROR`, P&R `CAP-FEATURE-FLAGS` (`formula`), Security `SEC-EXPORT-FORMULA-GUARD` (reconciled — export emits computed values), Architecture `COMPONENT-DATA-WORKER` | **38** |
| **CE-FORMULA-DEMO** — the ≥300k interacting (chained) formula-cell demo + full/incremental recalc timings, and the `PERF-FORMULA-NEUTRAL` re-run of the Part-2 baseline benches | deliverable | Performance `PERF-FORMULA-NEUTRAL` + `PERF-RECALC-*`, Quality `AC-FORMULA-*` + `formula/*.bench.ts`, P&R `SUCCESS-FORMULA` | **39** |

### v1.5 slices (built — promoted to a first-class opt-in capability)
| # | Slice | Type | Realizes | Guard | Built | Verified | Proof |
|---|---|---|---|---|:---:|---|---|
| 36 | **Formula engine**: A1 parser → AST, evaluator, ~70-function library, absolute/relative refs + ranges, seven typed error values; pure `evaluateFormula` | full | CE-FORMULA-ENGINE (`CAP-FORMULA`, `LIB-FORMULA-EVAL`, `ENTITY-FORMULA-CELL`/`-FORMULA-ERROR`, `FORMULA_PARSE_FAILED`/`FORMULA_DISABLED`, `SEC-FORMULA-NO-EVAL`) | PERF-FORMULA-NEUTRAL | ✅ | merge | Built `packages/core/src/formula/{tokenizer,parser,ast,references,values,eval-types,evaluator,functions,engine}.ts` (~70 functions). `AC-FORMULA-EVAL` proven by `formula/formula.test.ts` (24 tests: precedence, refs+ranges, seven error codes, `IFERROR`); `SEC-FORMULA-NO-EVAL` proven by the existing `security.static.test.ts` scan over the 392KB built bundle (no `eval(`/`new Function(`). 460 unit total. |
| 37 | **Recalc engine**: dependency graph (precedents/dependents by cell identity), Kahn full recalc + `#CIRC!` cycle marking, BFS incremental dirty-subgraph recalc; `recalculate()` | full | CE-FORMULA-RECALC (`CAP-FORMULA-RECALC`, `ENTITY-DEP-GRAPH`, `LIB-FORMULA-RECALC`, `EVT-AFTER-RECALC`/`-FORMULA-ERROR`, `INV-FORMULA-ACYCLIC`/`-INCREMENTAL`/`-REBUILD`) | PERF-RECALC-FULL / PERF-RECALC-INCR | ✅ | merge | Built the graph + Kahn full recalc (`#CIRC!` on unreached nodes) + BFS incremental dirty-subgraph recalc in `formula/engine.ts`; `recalculate()` wired via `MSG-RECALC`. `AC-FORMULA-CHAIN`/`-CYCLE`/`-INCREMENTAL` + `INV-FORMULA-*` proven in `formula/formula.test.ts` + `formula-grid.test.ts`. `formula/formula.bench.ts`: full recalc 100k ≈199ms; **leaf edit 3µs — 64,507× faster than full recalc** (incremental win). |
| 38 | **Grid wiring**: edit-seed → formula string (`getCellFormula`), `updateCell` routes a `=…` commit to the engine, computed value into `row.data[field]`, `EVT-AFTER-RECALC`/`-FORMULA-ERROR`, the `formula` feature flag | full | CE-FORMULA-GRID-WIRE (`LIB-FORMULA-ENTRY`/`-GET`, `INV-FORMULA-DERIVED`, `EVT-AFTER-RECALC`/`-FORMULA-ERROR`, `CAP-FEATURE-FLAGS`, `SEC-EXPORT-FORMULA-GUARD` reconciled) | PERF-FORMULA-NEUTRAL | ✅ | merge | Wired `IndexEngine.applyEdit`/`applyPaste` formula routing (computed value → `row.data[field]`), main-thread formula mirror for the edit-seed (`getCellValue`) + `getCellFormula`, `formula` feature flag (opt-in/default-off — reinterprets `=` values). `AC-FORMULA-DERIVED` proven by `formula-grid.test.ts` (getRows computed, editor sees formula, sort by computed, CSV emits result) + `e2e/formula.spec.ts` (6 real-DOM journeys incl. interactive `=formula` entry). |
| 39 | **Formula demo + perf**: a ≥300k chained-formula-cell demo reporting full + incremental recalc timings; re-run the Part-2 baseline benches with the module present | deliverable | CE-FORMULA-DEMO (`SUCCESS-FORMULA`, `PERF-RECALC-*`, `PERF-FORMULA-NEUTRAL`, `AC-FORMULA-DEMO`/`-NEUTRAL`) | PERF-FORMULA-NEUTRAL | ✅ | merge | Built `demo/formula.html` (interactive showcase) + `demo/formula-stress.html` (**300,000** chained cells incl. a 50k-deep running-Σ chain) + `e2e/perf/formula-perf.mjs`. `AC-FORMULA-DEMO`: full recalc 300k ≈454ms, head-cascade 114ms, leaf 17ms. `AC-FORMULA-NEUTRAL`: re-ran the Part-2 baselines — scroll p95 16.80ms (=), nodes 560 (=), paste 70.9ms, engine load/sort/filter 245/268/160ms, all P1–P14 guards intact (`perf/COMPARISON.md`). |
| 40 | **Worst-case demo**: identify the slowest built-in via a per-call micro-bench, chain it to 300k cells | deliverable | CE-FORMULA-DEMO (extends) — `PERF-RECALC-WORST`/`SEQ-RECALC-WORST`, `AC-FORMULA-WORST` | PERF-RECALC-WORST | ✅ | merge | Micro-bench ranked `COUNTIF`/`SUMIF`/`AVERAGEIF` slowest (~1.6µs/call, ~50× arithmetic — regex-per-range-cell in `matchCriteria`). `demo/formula-worst.html` = **300,000** chained `COUNTIF` cells (sliding window, vertical chain + horizontal cross-links → one A1 edit recomputes the whole graph). Full recalc **1292ms** (2.8× the arithmetic 300k), head-cascade **1366ms** (12×), leaf edit **16.8ms** (the incremental win survives the slow fn). E2E `e2e/formula.spec.ts -g 'worst case'`. |
| 41 | **Formula perf pass**: (a) compile `COUNTIF`/`SUMIF`/`AVERAGEIF` criteria once + numeric fast path; (b) numeric cell-graph keys | optimization | CE-FORMULA-OPT — `PERF-RECALC-WORST` re-baselined, `PERF-RECALC-FULL`/`-INCR` improved | PERF-RECALC-WORST / PERF-FORMULA-NEUTRAL | ✅ | merge | **(a)** `functions.ts` `compileCriteria` hoists the loop-invariant parse out of the range loop → COUNTIF **8.6×** / SUMIF·AVERAGEIF **10×** per call (**32.6×** on a 5k range — `countif.bench.ts` base-vs-after). **(b)** `engine.ts` `CellId` string→**number** (`rowIndex·2^14+colIndex`) → allocation-free `valueAt` → worst-case recalc **1292→755ms (1.7×)**, arithmetic **454→307ms**, heap **−20%**. Byte-identical (29 formula unit tests incl. 5 new criteria + 460 total + 7 E2E). `perf/OPTIMIZATION.md`. |

Bindings: the **build-new** contracts (`LIB-FORMULA-GET`/`-RECALC`/`-EVAL`, `EVT-AFTER-RECALC`/`-FORMULA-ERROR`, `ENTITY-FORMULA-CELL`/`-DEP-GRAPH`) now resolve to real `packages/core/src/formula/*` locators with `asserted_by` tests in `bindings.yaml`; **changed-but-bound** `LIB-UPDATE-CELL`/`COMPONENT-DATA-WORKER` keep their locators (formula routing added), `SEC-EXPORT-FORMULA-GUARD` reconciled (export emits computed values). `CAP-FORMULA`/`CAP-FORMULA-RECALC` transitive (not bound).

> **Origin & status.** This v1.5 delta was originally built as an autonomous POC to gauge the effort of adding Excel-like formulas to a grid that was never designed for them; it is now **promoted to a first-class, fully-supported capability**. It remains **feature-flag-gated (opt-in, default off)** — a deliberate design choice so a leading `=` stays literal text until a host opts in — so it stays fully additive: `PERF-FORMULA-NEUTRAL` holds (existing paths unchanged — see `perf/COMPARISON.md`). A post-build drift/adjudication pass (advisory) reconciled the docs with shipped behavior and made two product calls: `FORMULA_DISABLED` **degrades gracefully** (not thrown); an invalid pasted formula is **rejected per-cell** (Excel-like — `edit-session.ts` `applyBatch` fires `EVT-VALIDATION-ERROR` + drops the cell, the rest of the paste applies), never silently kept as literal text. Subsequently built on top of the v1.5 engine: structural-edit reference-rewriting (`INV-FORMULA-REBUILD`), the full function catalog (v1.6 → 455, completed v1.7 → 475), and array/spill formulas (`CAP-FORMULA-ARRAY`). Still `[FUTURE-SCOPE]` (documented in `docs/formula-support.md`): named ranges, whole-column/row refs, and writing live `=…` into `.xlsx` (export emits computed values). Cross-worksheet refs are **absent** (mini-grid has no worksheets).

## v1.6 delta — formula catalog & advanced capabilities (doc-feature)

The full Excel-comparable function catalog + three advanced formula capabilities authored **doc-only** (Contract-grade on the delta) into the concerns spanned by `docs/formula-support.md` (see its `# v1.6 delta` sections) + the new named catalog `docs/formula-functions.md`. Extends the built v1.5 engine: `CAP-FORMULA-FN` grows from the built ~70 to the **full ~230 pure functions**, plus three new capabilities — `CAP-FORMULA-VOLATILE` (volatile recalc), `CAP-FORMULA-REFVAL` (reference values), `CAP-FORMULA-ARRAY` (the Excel-style **spill engine**). All changes are **`additive`** — new `CAP-*`/`ENTITY-*`/`INV-*`/`EVT-*`/`PATTERN-*` breadth + two additive extensions to `ENTITY-FORMULA-CELL` (`volatile` field) / `ENTITY-FORMULA-ERROR` (`#SPILL!`/`#CALC!`); no v1.5 contract reshaped → **no manifest rung/scope change, no staleness**. **Since built (slices 42–45, ✅ merge — see the table), except the spill-surface remainder: 45d/45e are ◑ (the outline halo, the spill edit-guard, and the public `EVT-SPILL-CHANGE`/`LIB-FORMULA-SPILL` surface stay specced-unbuilt — the v1.6 spill-surface delta).**

| Change event (`source: doc-edit`) | Class | Owning concerns (refs) | Slice |
|---|---|---|---|
| **CE-FORMULA-CATALOG** — extend `CAP-FORMULA-FN` from the v1.5 ~70 to the **full ~230 pure** scalar/range functions (complete math/trig, statistical, financial, date/time, text, information, engineering, database categories) + the **`*IFS` family** (`SUMIFS`/`COUNTIFS`/`AVERAGEIFS`/`MAXIFS`/`MINIFS`); no new engine capability — each is `(args)=>value` in the `FUNCTIONS` registry. The exhaustive named list lives in `docs/formula-functions.md`. | additive | P&R `CAP-FORMULA-FN`(extended)/`SUCCESS-FORMULA-CATALOG`, Domain (no new entity — catalog reuses `ENTITY-FORMULA-CELL`), Quality `AC-FORMULA-CATALOG`, `docs/formula-functions.md` | **42** |
| **CE-FORMULA-VOLATILE** — new **`CAP-FORMULA-VOLATILE`**: functions that recompute on **every** recalc without a precedent changing (`NOW TODAY RAND RANDBETWEEN RANDARRAY OFFSET INDIRECT INFO CELL`); the incremental dirty closure is seeded with `edited ∪ volatileCells` | additive | P&R `CAP-FORMULA-VOLATILE`/`SUCCESS-FORMULA-VOLATILE`, Domain `ENTITY-FORMULA-CELL` (`volatile` field) + `INV-FORMULA-VOLATILE`, Performance `PERF-RECALC-VOLATILE`, Quality `AC-FORMULA-VOLATILE` | **43** |
| **CE-FORMULA-REFVAL** — new **`CAP-FORMULA-REFVAL`**: a **reference** value type in the evaluator enabling `OFFSET INDIRECT INDEX`(ref form)`/ADDRESS` (+ `ROW`/`COLUMN`/`ROWS`/`COLUMNS` over refs); `OFFSET`/`INDIRECT` are dynamic ⇒ volatile (`INV-REF-DYNAMIC-DEP`, binds to `CAP-FORMULA-VOLATILE`) | additive | P&R `CAP-FORMULA-REFVAL`/`SUCCESS-FORMULA-REFVAL`, Domain `ENTITY-CELL-REFERENCE` + `INV-REF-INGRID`/`INV-REF-DYNAMIC-DEP`, Interfaces (`A1#` ref surface), Quality `AC-FORMULA-REFVAL` | **44** |
| **CE-FORMULA-ARRAY** — new **`CAP-FORMULA-ARRAY`**: the Excel-style **spill engine** — a formula computing a rectangular array spills across the anchor + cells below/right (anchor-owns-range, mirroring `CAP-MERGE`); `FILTER SORT UNIQUE SEQUENCE …` + the `LAMBDA`/`LET`/`MAP`/`REDUCE` family; `#SPILL!`/`#CALC!` errors; the `A1#` spill reference. **The largest v1.6 capability — may span multiple slices.** | additive | P&R `CAP-FORMULA-ARRAY`/`SUCCESS-FORMULA-ARRAY`, Domain `ENTITY-SPILL-RANGE` + `ENTITY-FORMULA-ERROR` (`#SPILL!`/`#CALC!`) + `INV-SPILL-EMPTY`/`-PROJECTION`/`-NONOVERLAP`/`-ANCHOR-OWNS`, Interfaces `EVT-SPILL-CHANGE`, Architecture `PATTERN-SPILL` + `COMPONENT-DATA-WORKER`/`-RENDER`, Performance `PERF-RECALC-ARRAY`, Quality `AC-FORMULA-ARRAY-SPILL`/`-RESIZE`/`-DERIVED` | **45** |

### v1.6 slices — implementation-planner breakdown (slice 42 → 7 batches; 45 → 6 sub-slices)
Ordered plan from the `implementation-planner`. **Track A** (42a–g, the pure catalog) is engine-independent and fully parallel. **Track B** (43 → 44 → 45a…f) is the engine work, serial (45a may run alongside 44). Guard for every 42-batch: `AC-FORMULA-CATALOG` unit table + `PERF-FORMULA-NEUTRAL` (pure additions, off the hot path).

| # | Slice | Type | Realizes | Built | Verified | Proof |
|---|---|---|---|:---:|---|---|
| 42a | **`*IFS` + database batch** (19 fns): `SUMIFS COUNTIFS AVERAGEIFS MAXIFS MINIFS` (new `ifsReduce` — N `(range,criteria)` pairs, AND-per-row, reuses `compileCriteria`) + `COUNTBLANK SUBTOTAL` (1–11/101–111) + the 12 `D*` database fns (`DSUM DCOUNT DCOUNTA DGET DMAX DMIN DPRODUCT DAVERAGE DSTDEV DSTDEVP DVAR DVARP`). *(`AGGREGATE` → 42c-1, needs the 12–19 stats it dispatches.)* | full | CE-FORMULA-CATALOG (`CAP-FORMULA-FN`, `AC-FORMULA-CATALOG`) | ✅ | merge | Built `functions.ts` `ifsReduce`/`dbReduce`; **9 new `AC-FORMULA-CATALOG` cases** in `formula.test.ts` (multi-criteria AND, mismatched-shape→`#VALUE!`, `SUBTOTAL` codes, `DSUM/DGET/DVAR/…` incl. `DGET` >1-match→`#NUM!` + field-by-index). 471 unit; SEC-NO-EVAL intact; registry 83→102. |
| 42b | **Financial batch — core TVM/depreciation/conversion** (26 fns): `PMT PV FV NPER RATE IPMT PPMT CUMIPMT CUMPRINC ISPMT NPV IRR MIRR XNPV XIRR FVSCHEDULE SLN SYD DDB DB EFFECT NOMINAL PDURATION RRI DOLLARDE DOLLARFR` — annuity core + Newton/bisection solvers (`RATE`/`IRR`/`XIRR`). **42b-2 built** ✅ (20 keys, commit after 42e supplied the day-count machinery): `DISC PRICEDISC YIELDDISC INTRATE RECEIVED ACCRINTM TBILLPRICE/YIELD/EQ COUPNUM/PCD/NCD/DAYBS/DAYSNC/DAYS PRICE YIELD DURATION MDURATION VDB` — shared `dayCountFrac` (basis 0-4), `couponSchedule` (back-generate coupon dates), `bondPrice`/`bondDuration`, YIELD via bisection. 3 Excel-verified AC cases (PRICE 94.63436, YIELD 0.065, DURATION 5.9938, DISC, ACCRINTM, TBILL*, COUPNUM 4, VDB). Registry 396→416. *(Still deferred: `ODDF*/ODDL*` odd-period coupons, `AMORLINC/AMORDEGRC` French depreciation.)* | full | CE-FORMULA-CATALOG | ✅ | merge | Built `functions.ts` financial section; **5 `AC-FORMULA-CATALOG` cases** (Excel-verified `PMT/PV/FV/NPER`, `RATE`↔`PMT` inverse, `IPMT+PPMT===PMT`, `IRR/MIRR/XNPV/XIRR/FVSCHEDULE`, `SLN/SYD/DDB/DB`, `EFFECT/NOMINAL/RRI/DOLLARDE`). 476 unit; SEC-NO-EVAL intact; registry 102→128. |
| 42b-2 | **Financial — bond/coupon/day-count** (20 keys): `DISC PRICEDISC YIELDDISC INTRATE RECEIVED ACCRINTM TBILLEQ TBILLPRICE TBILLYIELD COUPDAYBS COUPDAYS COUPDAYSNC COUPNCD COUPNUM COUPPCD PRICE YIELD DURATION MDURATION VDB` — settlement/maturity dates + day-count basis (30/360, actual/actual, …) on the 42e date machinery | full | CE-FORMULA-CATALOG | ✅ | merge | Built after 42e (see the 42b row's "42b-2 built" note): `dayCountFrac` (basis 0–4) + `couponSchedule` + `bondPrice`/`bondDuration`, YIELD via bisection; 3 Excel-verified `AC-FORMULA-CATALOG` cases (PRICE 94.63436 / YIELD 0.065 / DURATION 5.9938 + DISC/ACCRINTM/TBILL*/COUPNUM/VDB). Registry 396→416. *(The then-deferred `ODDF*`/`ODDL*`/`ACCRINT`/`PRICEMAT`/`YIELDMAT` landed in **v1.7 slice 46**.)* |
| 42c-1 | **Statistical — descriptive + regression** (53 keys): `MEDIAN MODE.SNGL LARGE SMALL RANK.EQ/.AVG PERCENTILE.INC/.EXC QUARTILE.INC/.EXC PERCENTRANK.INC/.EXC STDEV.S/.P STDEVA/PA VAR.S/.P VARA/PA AVEDEV DEVSQ GEOMEAN HARMEAN KURT SKEW SKEW.P TRIMMEAN STANDARDIZE AVERAGEA/MAXA/MINA CORREL PEARSON COVARIANCE.P/.S SLOPE INTERCEPT RSQ STEYX FORECAST.LINEAR` + the compat aliases (`STDEV STDEVP VAR VARP MODE RANK QUARTILE PERCENTILE PERCENTRANK COVAR FORECAST`) + **`AGGREGATE`** (codes 1–19, ignore-errors options) | full | CE-FORMULA-CATALOG | ✅ | merge | Built `functions.ts` statistical section (percentile/rank/regression/skew-kurt helpers); **5 `AC-FORMULA-CATALOG` cases** over a standard dataset (Excel-verified descriptive/percentile/rank, means, regression `SLOPE/INTERCEPT/CORREL/RSQ/COVARIANCE/FORECAST`, `AGGREGATE` 1–19). 481 unit; SEC-NO-EVAL intact; registry 128→181. |
| 42c-2 | **Statistical — distributions + inverses + compat aliases** (62 keys): the `.DIST`/`.INV` family (`NORM.(S.)`, `LOGNORM`, `EXPON`, `GAMMA`, `BETA`, `CHISQ(.RT)`, `T(.RT/.2T)`, `F(.RT)`, `BINOM(.INV)`, `NEGBINOM`, `POISSON`, `HYPGEOM`, `WEIBULL`) + `CONFIDENCE.NORM/.T Z.TEST GAUSS PHI FISHER FISHERINV` + ~24 legacy aliases. Toolkit: regularized incomplete gamma/beta, Acklam inverse-normal, bisection inverses. *(Deferred: array `T.TEST/F.TEST/CHISQ.TEST` + `FORECAST.ETS*`.)* | full | CE-FORMULA-CATALOG | ✅ | merge | Built `functions.ts` distributions section; **4 `AC-FORMULA-CATALOG` cases** (Excel-verified normal/gamma/chisq/poisson/t/F/beta/binom + inverse round-trips + CONFIDENCE/Z.TEST/FISHER). 502 unit; registry 316→378. **Track A (pure catalog) complete.** |
| 42d | **Math/trig batch** (53 keys): `PI DEGREES RADIANS SQRTPI` + full trig (`SIN COS TAN ASIN ACOS ATAN ATAN2 SEC CSC COT ACOT`) + hyperbolic (`SINH…ACOTH`) + `GCD LCM QUOTIENT EVEN ODD FACT FACTDOUBLE COMBIN COMBINA PERMUT PERMUTATIONA MULTINOMIAL GAMMALN SERIESSUM SUMX2MY2/X2PY2/XMY2 ROMAN ARABIC BASE DECIMAL MROUND CEILING.MATH/.PRECISE FLOOR.MATH/.PRECISE ISO.CEILING` (`RAND`/`RANDBETWEEN` → slice 43) | full | CE-FORMULA-CATALOG | ✅ | merge | Built `functions.ts` math section (Lanczos `gammaln`, overflow-safe `COMBIN`/`PERMUT`, roman/base converters, sign-aware `CEILING/FLOOR.MATH`); **4 `AC-FORMULA-CATALOG` cases** (trig+domain, combinatorics, series/paired-sums, roman/base/rounding). 485 unit; SEC-NO-EVAL intact; registry 181→234. |
| 42e | **Date/time + Text batch** (32 keys): `TIME HOUR MINUTE SECOND DATEVALUE TIMEVALUE DATEDIF DAYS DAYS360 EDATE EOMONTH WEEKDAY WEEKNUM ISOWEEKNUM YEARFRAC NETWORKDAYS NETWORKDAYS.INTL WORKDAY WORKDAY.INTL` (serial-date helpers, EDATE/EOMONTH month-end clamp, weekend-set for `.INTL`) + `FIXED DOLLAR NUMBERVALUE CLEAN T UNICHAR UNICODE TEXTBEFORE TEXTAFTER VALUETOTEXT REGEXEXTRACT REGEXREPLACE REGEXTEST` | full | CE-FORMULA-CATALOG | ✅ | merge | Built `functions.ts` date/time + text sections; **6 `AC-FORMULA-CATALOG` cases** (time parts, EDATE/EOMONTH clamp, DATEDIF, WEEKDAY, YEARFRAC/ISO-week, NETWORKDAYS/WORKDAY, FIXED/DOLLAR/NUMBERVALUE, TEXTBEFORE/AFTER, REGEX*). 491 unit; registry 234→266. **`[REVISIT]`:** `FIXED`/`DOLLAR` use default en-US; routing them + date formatters through `COMPONENT-I18N` locale is a small follow-up (needs a `locale` on `CellResolver`). **Unblocks 42b-2** (bond/coupon day-count now has date machinery). |
| 42f | **Engineering batch** (39 keys): base conversions `DEC2BIN/OCT/HEX BIN2DEC/OCT/HEX OCT2* HEX2*` (10-digit two’s-complement) + bitwise `BITAND/OR/XOR/LSHIFT/RSHIFT` (BigInt, ≤2⁴⁸) + `DELTA GESTEP ERF ERF.PRECISE ERFC ERFC.PRECISE CONVERT` (mass/dist/time + temperature) + complex `COMPLEX IMREAL IMAGINARY IMABS IMARGUMENT IMCONJUGATE IMSUM IMSUB IMPRODUCT IMDIV IMSQRT IMEXP IMLN IMSIN IMCOS` | full | CE-FORMULA-CATALOG | ✅ | merge | Built `functions.ts` engineering section (base/bitwise/CONVERT/complex-parse+format/erf); **4 `AC-FORMULA-CATALOG` cases**. 495 unit; registry 266→305. *(Deferred: `BESSEL*`, the full CONVERT unit table + metric prefixes, and the remaining `IM*` trig/log — heavy/niche.)* |
| 42g | **Lookup-pure + Info-pure batch** (11 keys): `XLOOKUP`(single-result, match-modes 0/±1)` XMATCH LOOKUP`(vector)` ADDRESS`(A1 + R1C1)` HYPERLINK`(display)` ISODD ISEVEN ISLOGICAL ISNONTEXT TYPE ERROR.TYPE`. Also **enhanced the parser** to accept **omitted arguments** (`,,`) as a `missing` node → blank, so `XLOOKUP(x,l,r,,-1)` parses; `fnum` treats omitted/blank optional args as their default. *(`ISFORMULA FORMULATEXT ISREF AREAS CELL` + `ROW/COLUMN`(ref) need reference-arg handling → **slice 44**.)* | full | CE-FORMULA-CATALOG | ✅ | merge | Built `functions.ts` lookup/info section + `ast.ts`/`parser.ts`/`evaluator.ts` omitted-arg node; **3 `AC-FORMULA-CATALOG` cases** (XLOOKUP match-modes/if-not-found, ADDRESS A1/R1C1, info predicates + TYPE/ERROR.TYPE). 498 unit; registry 305→316. |
| 43 | **Volatile recalc**: `volatile` flag (derived at parse via `astHasCall`) + `volatileCells` index + dirty-closure seeding `edited ∪ volatileCells`; add `RAND RANDBETWEEN` + `VOLATILE_FUNCTIONS` set (RAND/RANDBETWEEN/RANDARRAY/NOW/TODAY/OFFSET/INDIRECT/INFO/CELL) | full | CE-FORMULA-VOLATILE (`CAP-FORMULA-VOLATILE`, `ENTITY-FORMULA-CELL.volatile`, `INV-FORMULA-VOLATILE`, `PERF-RECALC-VOLATILE`, `AC-FORMULA-VOLATILE`) | ✅ | merge | Built `engine.ts` volatile tracking (flag on `FormulaCell`, `volatileCells` set maintained across set/clear/rebuild, `recalcFrom` folds volatiles into every seed) + `ast.ts` `astHasCall` + `functions.ts` RAND/RANDBETWEEN. **2 `AC-FORMULA-VOLATILE` cases** (volatile recomputes on unrelated recalc; non-volatile stays put, `changed=0`). 504 unit; registry 378→380; SEC-NO-EVAL intact (`Math.random` is allowed). |
| 44 | **Reference values**: `ReferenceValue` in `EvalResult` union + `isReference`; `ref`/`range` nodes now evaluate to lazy references, deref'd at every non-ref-aware boundary (`evaluate`/`scalar`/binary operands/ordinary fn args); `REF_AWARE_FUNCTIONS` set passes raw refs. `OFFSET INDIRECT INDEX`(ref form, 0=whole row/col) `ROW/COLUMN/ROWS/COLUMNS`(ref) `ISFORMULA FORMULATEXT ISREF CELL`; resolver gains `formulaSourceAt`. OFFSET/INDIRECT are volatile (slice 43). | full | CE-FORMULA-REFVAL (`CAP-FORMULA-REFVAL`, `ENTITY-CELL-REFERENCE`, `INV-REF-INGRID`/`-DYNAMIC-DEP`, `AC-FORMULA-REFVAL`) | ✅ | merge | Built `eval-types.ts` (ReferenceValue + resolver hooks), `evaluator.ts` (lazy-ref + deref), `functions.ts` (OFFSET/INDIRECT/INDEX-ref/ISFORMULA/FORMULATEXT/ISREF/CELL + ref-aware ROW/COLUMN), `engine.ts` (`formulaSourceAt`). **4 `AC-FORMULA-REFVAL` cases** (OFFSET→value/range/#REF!, INDIRECT, INDEX 0=whole line, ROW/COLUMN(ref), ISFORMULA/FORMULATEXT read sidecar). 508 unit; registry 380→386; existing 67 formula tests unregressed. |
| 45a | **Arrays as in-formula values** (non-spilling): array return consumed inside reducers (`SUM(FILTER(…))`, `INDEX(SORT(…))`); added `#SPILL!`/`#CALC!` error codes; `SEQUENCE RANDARRAY TRANSPOSE UNIQUE FILTER SORT SORTBY TEXTSPLIT TOROW TOCOL` (return `RangeValue`); INDEX array-form fixed for single-column vectors. **Reshape helpers built** ✅ (11 keys): `TAKE DROP HSTACK VSTACK WRAPROWS WRAPCOLS CHOOSEROWS CHOOSECOLS EXPAND MMULT FREQUENCY` (on the `asGrid`/`makeRange` helpers). Registry 416→427. | headless | CE-FORMULA-ARRAY (array eval, `ENTITY-FORMULA-ERROR` `#CALC!`) | ✅ | merge | Built `functions.ts` array section (`makeRange`/`to2D`/`valueKey`/`truthy`) + `values.ts` #SPILL!/#CALC!; **2 `AC-FORMULA-ARRAY` cases** (SEQUENCE/TRANSPOSE/TEXTSPLIT; UNIQUE/SORT/FILTER over a column). 510 unit; registry 386→396. |
| 45b | **`LET`/`LAMBDA` + iteration**: `name` AST node (bare identifiers survive parsing) + lexical `Scope` threaded through the evaluator + `LambdaValue` closure (params+body+captured scope) in `EvalResult`; special forms `LET LAMBDA MAP REDUCE SCAN BYROW BYCOL MAKEARRAY` handled in the evaluator (unevaluated arg ASTs). Lambda callable via LET binding (`f(...)`); bare lambda / array-in-scalar → `#CALC!`. *(Deferred: BYCOL variants tested via BYROW; ISOMITTED.)* | headless | CE-FORMULA-ARRAY (lambda/scoping) | ✅ | merge | Built `ast.ts` (name node), `parser.ts` (emit name), `eval-types.ts` (LambdaValue/Scope/isLambda), `evaluator.ts` (scope threading + `evalSpecialForm` + `applyLambda` + `collapse`/`asRange` + `evaluateResult`). **3 `AC-FORMULA-ARRAY` cases** (LET sequential bind; LAMBDA via LET + bare→#CALC!; MAP/REDUCE/SCAN/MAKEARRAY/BYROW). 513 unit; existing tests unregressed. |
| 45c | **Spill materialization** (`PATTERN-SPILL`): a top-level array result projects into anchor + below/right via `writeDisplay`; blocking pass (literal / other formula / other anchor's spill) → `#SPILL!`; `spillRegions`+`spilledInto` indices; prior region cleared before recompute (resize/shrink). | headless | CE-FORMULA-ARRAY (`ENTITY-SPILL-RANGE`, `INV-SPILL-*`, `PERF-RECALC-ARRAY`) | ✅ | merge | Built `engine.ts` `computeAndMaterialize`/`spill`/`clearSpill` (via `evaluateResult`), spill state cleared in clearFormula/clear/rebuild/cycle path. **5 `AC-FORMULA-ARRAY` cases** (1-D spill, 2-D spill, #SPILL! block + literal preserved, shrink clears vacated cell, spilled cell feeds a downstream formula). 518 unit. |
| 45d | **Spill render** `[REVISIT]`: spilled values already project into `row.data` (via `writeDisplay`), so the **existing renderer shows them** — the derived pipeline (`INV-CELL-DERIVED`) makes 45c materialization visible with no renderer change. *Remaining:* the spill **outline halo** (`data-mg-spill`) + edit-block are cosmetic host-render follow-ups. | full | CE-FORMULA-ARRAY (`COMPONENT-RENDER`, `AC-FORMULA-ARRAY-SPILL` UI + E2E) | ◑ | merge | Values visible via derived projection (covered by 45c); outline styling + edit-guard deferred (cosmetic). |
| 45e | **Resize + `A1#`** ✅ + `EVT-SPILL-CHANGE` `[REVISIT]`: array resize/re-project on recompute (45c) **and the `A1#` spill-reference operator** are now built. Tokenizer emits a bare `#` op (falls back from error-literal), parser produces a `spillref` AST node after a ref, `collectRefs` registers the anchor as a precedent (so `=SUM(A1#)` recalcs when the anchor re-spills), evaluator resolves it via `resolver.spillExtentAt` → the engine's `spillShape` map. *Remaining:* a formal `EVT-SPILL-CHANGE` event for host observers (additive). | full | CE-FORMULA-ARRAY (`EVT-SPILL-CHANGE`, `AC-FORMULA-ARRAY-RESIZE`) | ◑ | merge | `A1#` built (tokenizer/ast/parser/evaluator/engine `spillShape`); **`AC-FORMULA-ARRAY`**: `A1#` no-spill→anchor, `=SUM(A1#)`→live extent, shrink auto-updates the dependent. 524 unit. EVT-SPILL-CHANGE still deferred. |
| 45f | **Derived-pipeline reconciliation**: spilled cells write through `writeDisplay` into `row.data[field]` — the **same derived projection every formula cell uses** — so spilled values participate in `INV-CELL-DERIVED` and are read back by dependents (`=A2` reads a spilled A2). | full | CE-FORMULA-ARRAY (`AC-FORMULA-ARRAY-DERIVED`, extends `INV-FORMULA-DERIVED`) | ✅ | merge | Achieved by construction in 45c (materialization uses the derived write path); the "spilled cell feeds a downstream formula" AC covers it. Sort/filter-view outline over runs `[REVISIT]`. |

**Build order:** Track A `42a→42b→42c-1→42c-2→42d→42e→42f→42g` (parallel) · Track B `43 → 44 → 45a → 45b → 45c → 45d → 45e → 45f` (45a may run alongside 44). **`[REVISIT]` at build:** array-vs-`RangeValue` unification (45a); spill bypasses column `validation` (45c); spill outline under a sorted view (45f). Bindings stubs name the new sites (`formula/reference.ts`, `formula/spill.ts`, `event-bus.ts#spillChange`).

## v1.7 delta — catalog completion (doc-feature)

The remaining `◻` catalog gaps promoted from deferred to Contract-grade (docs-first: `docs/formula-support.md` "v1.7 delta") and **built**: the ~20 deferred functions (registry **455 → 475**) + the implicit-intersection **`@` operator** (`CAP-FORMULA-INTERSECT`, the one engine change). All **`additive`** — new registry entries + one operator capability; no v1.5/v1.6 contract reshaped → **no manifest rung/scope change, no staleness**. `INFO` was **dropped from scope (wholly ABSENT** — unregistered → `#NAME?`; every `INFO(type)` needs host/OS/workbook context mini-grid does not have). The catalog is now honestly closed except the enumerated `✗`-ABSENT set (~30, each blocked by an architectural fact).

| Change event (`source: doc-edit`) | Class | Owning concerns (refs) | Slice |
|---|---|---|---|
| **CE-CATALOG-V17** — the deferred functions: bucket A quick wins (`MODE.MULT PROB AREAS` array-`XLOOKUP` `ARRAYTOTEXT`), bucket B array math (`TREND GROWTH LINEST LOGEST MINVERSE MDETERM MUNIT`), bucket C odd-period bonds (`ACCRINT PRICEMAT YIELDMAT ODDFPRICE ODDFYIELD ODDLPRICE ODDLYIELD`), bucket D `GROUPBY`/`PIVOTBY` (aggregation mini-DSL) + `ISOMITTED` (9th special form); array-result fns reuse the built spill machinery (return `RangeValue`) | additive | P&R `CAP-FORMULA-FN`(completed)/`SUCCESS-FORMULA-CATALOG`, Quality `AC-CATALOG-V17`, `docs/formula-functions.md` (tag source) | **46** |
| **CE-FORMULA-INTERSECT** — new **`CAP-FORMULA-INTERSECT`**: the prefix `@` (implicit intersection) operator — tokenizer/parser/AST/evaluator extension; `@x` always scalar or `#VALUE!`, never spills (`INV-INTERSECT-SCALAR`) | additive | P&R `CAP-FORMULA-INTERSECT`/`SUCCESS-FORMULA-INTERSECT`, Domain `INV-INTERSECT-SCALAR`, Architecture (parser/evaluator extension), Quality `AC-FORMULA-INTERSECT` | **47** |

### v1.7 slices (built)
| # | Slice | Type | Realizes | Built | Verified | Proof |
|---|-------|------|----------|:---:|---|---|
| 46 | **Catalog completion** (buckets A–D + `ISOMITTED`): registry 455→475 in `formula/functions.ts` (shared linear-algebra core, quasi-coupon-period schedule on `dayCountFrac`/`couponSchedule`, `GROUPBY`/`PIVOTBY` reducer DSL + `LAMBDA` aggregation); `INFO` dropped (ABSENT, unregistered) | full | CE-CATALOG-V17 (`CAP-FORMULA-FN`, `AC-CATALOG-V17`) | ✅ | merge | **Unit tier (the ACs' contracted tier — no new public `LIB-*`/`EVT-*`/`DOM-*`, so no E2E owed):** `AC-CATALOG-V17` bucket A–D blocks in `formula/formula.test.ts` (array fns assert spilled shape+values; `GROUPBY`/`PIVOTBY` match Excel group results incl. `LAMBDA` aggregation; `ODDF*`/`ODDL*`/`PRICEMAT`/`YIELDMAT` round-trip yield↔price). Registry count **475** verified programmatically 2026-07-12; full unit suite **563 green** (re-run 2026-07-12). `SEC-NO-EVAL` scan unaffected. |
| 47 | **Implicit-intersection `@`** (`CAP-FORMULA-INTERSECT`): `intersect` AST node across `formula/{tokenizer,parser,ast,evaluator}.ts` (prefix, binds looser than range `:`; survives `translateAst`/`formatAst`) | full | CE-FORMULA-INTERSECT (`INV-INTERSECT-SCALAR`, `AC-FORMULA-INTERSECT`) | ✅ | merge | **Unit tier:** the `CAP-FORMULA-INTERSECT — the @ operator` block in `formula/formula.test.ts` (`AC-FORMULA-INTERSECT`: row/column intersection, 1×1 identity, no-intersection → `#VALUE!`, never-spills scalar usable in arithmetic, `formatAst` round-trip). Green in the 2026-07-12 suite run. |
| — | **Solver-opt follow-up** (perf pass over the new catalog): `FORECAST.ETS` seasonal-buffer reuse + SSE fold, root-finder pass over yield solvers | optimization | `PERF-RECALC-WORST` re-baselined | ✅ | merge | `perf/OPTIMIZATION.md` (second pass — FORECAST.ETS 2.15×); `PERF-RECALC-WORST` **retargeted to ~12k chained `FORECAST.ETS` cells, re-baselined ≈967ms** (Performance doc + `formula/formula-fn.bench.ts`). |

Bindings: the bucket functions are `CAP-FORMULA-FN` registry entries (curated — no per-fn binding); `CAP-FORMULA-INTERSECT` is transitive (`CAP-*` not bound, per the `bindings.yaml` header); `INV-INTERSECT-SCALAR` is asserted by the named unit block (see the `bindings.yaml` v1.7 comment). **Not part of v1.7:** the v1.6 **spill-surface delta** (public `EVT-SPILL-CHANGE`/`LIB-FORMULA-SPILL`, outline, edit-guard) remains **specced, build pending** (45d/45e above) — run `implementation-planner` on that delta next.

## Capability coverage
✅ **All 32 in-scope `CAP-*` are built and verified at merge** (each user-facing capability with its real-flow E2E — no capability built in a single layer; the engine-tier formula capabilities at their contracted unit/bench tiers): the 23 v1 caps `CAP-DATA-BIND`, `-VIRTUALIZE`, `-EDIT`, `-VALIDATE`, `-UNDO`, `-SELECT`, `-CLIPBOARD`, `-FMT-CELL`, `-FMT-VALUE`, `-COND-FMT`, `-SORT`, `-FILTER`, `-RESIZE`, `-REORDER`, `-FREEZE`, `-MERGE`, `-GROUP`, `-EXPORT`, `-PERSIST-STATE`, `-THEME`, `-I18N`, `-A11Y`, `-FEATURE-FLAGS`. **v1.1 (built, slices 13–15):** select-editor popover fix, boolean commit-on-change fix, real-worker seam. **v1.2 (doc-led):** `CAP-ROW-HEADER` (slice 16) — **RETIRED**, superseded by the v1.3 `CAP-HEADER` (never built). **v1.3/v1.4 (built, slices 17–21):** `CAP-HEADER`, `CAP-COLUMN-MANAGE`, `CAP-MENU` (`CAP-HEADER-MENU` **retired** → `CAP-MENU`) + `CAP-SELECT` reshaped to full multi-range (**breaking**, reconciled — staleness cleared). **v1.5 (built, slices 36–41):** `CAP-FORMULA` + `CAP-FORMULA-FN`. **v1.6 (built, slices 42–45):** `CAP-FORMULA-VOLATILE`, `CAP-FORMULA-REFVAL`, `CAP-FORMULA-ARRAY` — *except the spill-surface delta (45d/45e ◑): outline, edit-guard, public `EVT-SPILL-CHANGE`/`LIB-FORMULA-SPILL` remain specced-unbuilt.* **v1.7 (built, slices 46–47):** `CAP-FORMULA-FN` completed (475) + `CAP-FORMULA-INTERSECT`. **[FUTURE-SCOPE] v2 (outside gate):** async DataSource adapter, multi-sheet, server-side ops, crash-recovery edit-replay, header aggregation/summary row.
