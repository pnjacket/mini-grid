# e2e

Placeholder for the end-to-end and accessibility test suite.

Specs here run under Playwright (`pnpm e2e`, config at `playwright.config.ts`)
and use `@axe-core/playwright` for automated a11y checks against the `demo/`
app. Real specs land in a later slice.
